# se crea la clase A
# merge main con develop v2"

class ClaseA {
        constructor(foo,bar){
                this.foo = "foo";
                this.bar = "bar";
                }
        displayInfo( {

                console.log(`esto es ${this.foo} y eso es ${bar} v2`);
                }
        }

