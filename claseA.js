# se crea la clase A
# merge main con develop v2"
# se añade atributo lorem

class ClaseA {
        constructor(foo,bar, lorem){
                this.foo = "foo";
                this.bar = "bar";
		this.lorem = "lorem"
                }
        displayInfo( {

                console.log(`esto es ${this.foo}, esto es ${this.bar} y esto es lorem ${this.lorem}`);
                }
        }

